# Claude Project Notes

Primary repo guidance lives in [AGENT.md](/d:/Workspace/Dart/Spritesheet-Creator/AGENT.md).

Key points:

- SpriteCraft is a pure Dart project with a Shelf-served web Studio.
- `lpc-spritesheet-creator/` is a git submodule dependency for LPC definitions and spritesheets.
- Do not assume `TODO.md` is current; verify against code.
- Run `git submodule update --init --recursive` and `dart pub get` before deeper debugging.

