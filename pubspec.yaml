# File: pubspec.yaml

name: spritecraft
description: Pure Dart CLI and library for crafting spritesheets, with LPC-powered composition and Gemini-ready AI planning hooks.
version: 0.4.12
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0-113.2.beta

# Add regular dependencies here.
dependencies:
  archive: ^4.0.9
  args: ^2.7.0
  http: ^1.5.0
  image: ^4.5.4
  mime: ^2.0.0
  path: ^1.9.1
  postgres: ^3.5.9
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
