# SpriteCraft v0.3.0 — Implementation TODO

## Backend

- [ ] `lib/src/persistence/history_repository.dart` — add `findById()` and `delete()`
- [ ] `lib/src/server/studio_server.dart` — add `GET /api/history/:id`, `DELETE /api/history/:id`, `POST /api/history/restore`

## Frontend

- [ ] `studio/favicon.svg` — create SVG favicon
- [ ] `studio/index.html` — favicon link, toast container, Clear-all button, selection count badge
- [ ] `studio/styles.css` — toast, spinner, restore-btn, clear-btn styles
- [ ] `studio/app.js` — catalog item cache, fix renderSelections names, toast system, loading states, history restore, clear-all, keyboard shortcuts, better export feedback

## Docs

- [ ] `CHANGELOG.md` — add 0.3.0 entry
